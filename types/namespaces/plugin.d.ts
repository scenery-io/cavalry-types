declare namespace plugin {
  /**
   * The Layer ID of the plugin.
   */
  const layerId: string;
  /**
   * `saveData` is a copy of the data found in the .cv file for this Layer which can be used to access attribute data.
   */
  const saveData: unknown;
}
